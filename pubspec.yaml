name: atlas_fitness
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  firebase_auth: ^5.1.2
  firebase_core: ^3.2.0
  firebase_storage:
  cloud_firestore: 
  provider:
  curved_navigation_bar: ^1.0.6
  cupertino_icons:
  textfield_tags: ^3.0.1
  #image_picker: ^1.0.0  

  flutter:
    sdk: flutter
  timeline_tile:
  flutter_svg: 
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/logos/
    - lib/images/illustrations/
    - lib/images/meals_images/
    - lib/images/workout_images/